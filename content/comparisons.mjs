export function comparisons({eyebrow,img}) {
  return `<section class="section transformations" id="vorher-nachher"><div class="container"><div class="section-heading"><div>${eyebrow('Vorher & Nachher')}<h2>Von Kartons zu fertigen Möbeln</h2><p>Ziehen Sie die Trennlinie direkt im Bild – von den Kartons zum fertig montierten Möbelstück.</p></div></div><div class="comparison-grid">${[['karton.jpg','Bettt2.jpg','Bettmontage'],['photo222.jpg','g444.jpg','IKEA BESTÅ']].map(([before,after,name],i)=>`<article class="comparison"><div class="comparison-images" style="--reveal:50%"><div class="comparison-after-layer">${img(after,name+' – nach der Montage')}</div><div class="comparison-before-layer">${img(before,name+' – vor der Montage')}</div><span class="comparison-line" aria-hidden="true"><span class="comparison-grip">‹<span></span>›</span></span><span class="comparison-tag before">Vorher</span><span class="comparison-tag after">Nachher</span><input class="comparison-control" id="comparison-${i}" type="range" min="0" max="100" value="50" aria-label="Vorher-Nachher-Vergleich ${name}" aria-valuetext="50 Prozent Vorher, 50 Prozent Nachher"></div><div class="comparison-caption"><h3>${name}</h3><label for="comparison-${i}">Ziehen & vergleichen <span aria-hidden="true">↔</span></label></div></article>`).join('')}</div></div></section>`;
}
