# Browser key for live Google reviews. Restrict it in Google Cloud to the
# production domains and enable Maps JavaScript API + Places API (New).
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# Public Web3Forms form identifier. Restrict saarmontage.de in the Web3Forms
# dashboard when the account plan supports domain restriction.
NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY=
